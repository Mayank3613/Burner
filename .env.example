# Burner Environment Configuration
# Burner runs completely locally and offline. No API keys are required.
# You can optionally configure cache directory overrides:

# HF_HOME=~/.cache/huggingface
# BURNER_MIN_CONFIDENCE_FLOOR=0.55
